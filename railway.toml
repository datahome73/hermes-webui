[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildArgs = { HERMES_VERSION = "0.51.58" }

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ALWAYS"
numReplicas = 1
